<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="/edit_article.css">
</head>

<body>
    <!-- Header section with navigation links -->
    <header>
        <div class="container">
            <h1>Edit Article</h1>
            <nav>
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/reader">Reader Home</a></li>
                    <li><a href="/add_user">Register</a></li>
                    <li><a href="/login">Login</a></li>
                    <li><a href="/profile">Profile</a></li>
                </ul>
            </nav>
        </div>
    </header>
    <main>
        <div class="container">
            <section>
                <h2>Edit Article</h2>
                <!-- Form for editing the article -->
                <form action="/profile/article/edit/<%= article.id %>" method="POST">
                    <!-- Displaying article meta information -->
                    <div class="article-info">
                        <div class="meta-info">
                            <p>
                                <%= article.readCount %> Viewers &#8226; <span id="like-count">
                                        <%= article.likeCount %>
                                    </span> Likes
                            </p>
                        </div>
                        <p>Created: <%= article.createdDate %>
                        </p>
                        <% if (article.status==='published' ) { %>
                            <p>Published Date: <%= article.publishedDate %>
                            </p>
                            <% } %>
                    </div>
                    <!-- Input for editing the article title -->
                    <label for="title">Title:</label>
                    <input type="text" id="title" name="title" value="<%= article.title %>" required>
                    <br><br>
                    <!-- Textarea for editing the article content -->
                    <label for="content">Content:</label>
                    <textarea id="content" name="content" rows="8" required><%= article.content %></textarea>
                    <br><br>
                    <!-- Buttons for saving changes and going back to the profile page -->
                    <button type="submit">Save Changes</button>
                    <button class="btn btn-secondary" onclick="window.location.href='/profile'">Back</button>
                </form>
            </section>
        </div>
    </main>
    <footer>
        <div class="container">
            <p>&copy; 2024 Your Blog. All rights reserved.</p>
        </div>
    </footer>
</body>

</html>